= simple_form_for(@shop) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.association :country
    = f.input :state_id, collection: Country.all, as: :grouped_select, group_method: :states

  .form-actions
    = f.button :submit
